---
layout: base.njk
---
<div class="row">
	<div class="column">
		<h2>{{ title }}</h2>
		<h2>{{ title2 }}</h2>
		<img src="{{ featuredImage | url }}" alt="{{ featuredImage }}">
		<p>{{ content | safe }}</p>
	</div>
</div>